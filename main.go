package main

func main() {
	// config := config.NewConfig()
	// db := config.DbConnect()

	// transactionRepo := repository.NewTransactionRepository(db)
	// transactionUsecase := usecase.NewTransactionUseCase(transactionRepo)

	// router := gin.Default()

	// // menambahkan route untuk mengambil saldo
	// router.POST("/saldo/:id", func(c *gin.Context) {
	// // 	var addDompet *model.Balance
	// // 	saldo, err := transactionUsecase.AddWallet()
	// // 	if err != nil {
	// // 		c.JSON(500, gin.H{"error": err.Error()})
	// // 		return
	// // 	}

	// // 	// mengirimkan saldo sebagai response
	// // 	c.JSON(200, gin.H{"saldo": saldo})
	// // })

	// // menjalankan server
	// router.Run(":8080")

}
